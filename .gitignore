node_modules/
dist/
.firebase/
service-account.json
*firebase-adminsdk*.json
*.log
.DS_Store

# Historical build/source snapshots (not needed in version control)
iworship-deploy-v*.zip
iworship-full-source-v*.zip
dist.zip

# scripts/build-devotionals.mjs's fetch cache -- raw scraped HTML, not
# source we own; devotionals.json (the actual built output, in
# src/content/ and functions/) IS committed as normal.
.devotional-cache/
