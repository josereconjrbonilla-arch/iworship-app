{
  "name": "iworship-functions",
  "description": "Cloud Functions scaffold for iWorship's monetization backend -- see README.md in this folder before touching anything here.",
  "private": true,
  "engines": { "node": "22" },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.0.0"
  }
}
